CC=@CC@
CFLAGS=@CFLAGS@
MAKEFLAGS=-j
%.o: %.c $(shell ls *.h)
	$(CC) $(CFLAGS) -c $<
OBJ=$(shell ls *.c|sed -e 's,.c$$,.o,g')
dualcount: $(OBJ)
	$(CC) $(CFLAGS) -o $@ $(OBJ)
clean:
	rm -f `cat .gitignore`
